# flavors-bsc-referral-bot

[Flavors BSC](https://flavorsbsc.com/) Telegram Bot for tracking referrals.

## Getting Started

```bash
# Update config.json settings
$ cp -a config.example.json config.json

# Run the bot via Docker
$ docker-compose up -d
```
